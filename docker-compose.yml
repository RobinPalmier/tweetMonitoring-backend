version: "3.3"
services:
  node:
    image: node:latest
    ports:
      - '5000:5000'
    volumes :
      - './:/app'
    command:
      - '/bin/bash'
      - '-c'
      - 'npm install && npm start'
    container_name: 'tweetmonitoring-backend'
    working_dir: '/app'
